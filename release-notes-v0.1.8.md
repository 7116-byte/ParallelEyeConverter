# v0.1.8

调整控制条和旋转遮罩。

- 最大化按钮改为打开本软件主页；为避免主页被遮住，会先把分屏最小化为悬浮球。
- 控制条自动隐藏时间从 3 秒改为 2 秒。
- 分屏悬浮窗改为 `MATCH_PARENT` 全屏覆盖，不再使用旋转瞬间可能过期的固定屏幕宽高。
- 分屏窗口格式改为不透明黑底，减少旋转时露出原 App 内容。
- APK 文件名为 `ParallelEyeConverter-v0.1.8-debug.apk`。
