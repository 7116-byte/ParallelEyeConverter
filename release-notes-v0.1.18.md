# v0.1.18 不透明触摸穿透试验

- 双击全屏画面改为进入 `透` 模式，不再切换横竖显示。
- `横 / 竖` 显示模式保留为控制条按钮切换。
- `透` 模式下画面保持完全不透明，尝试实现只穿透触控、不叠加底层画面的效果。
- 控制条上的 `控` 按钮仍用于退出穿透并恢复控制模式。

说明：Android 12+ 对不透明悬浮窗触摸穿透有限制，如果部分设备无法滑动底层 App，需要回退到半透明穿透方案。
