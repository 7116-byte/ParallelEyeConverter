# v0.1.9 前台目标应用回跳修复

- 修复点击悬浮球后没有先回到授权应用、而是直接在当前界面分屏的问题。
- 前台识别从短时间单次事件改为读取最近使用记录，优先保存最近的真实内容应用，过滤本应用、系统界面和桌面。
- 点击悬浮球时，只要有已保存目标应用，即使当前前台包名暂时读不到，也会先拉起目标应用。
- 拉起目标应用后等待 700ms 再显示分屏，减少进入过早导致的黑屏或错误捕获。
- 版本号升级到 `0.1.9`。

