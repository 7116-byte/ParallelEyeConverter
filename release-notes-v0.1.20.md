# v0.1.20 右侧触控通道

- `透` 模式改为右侧触控通道，不再让整屏 SBS 窗口进入触摸穿透。
- SBS 主画面保持不透明，右侧留出一条 86dp 区域给底层 App 接收上滑手势。
- 双击 SBS 主画面可以进入或退出 `透` 模式。
- `透` 模式下仍保留右侧小 `控` 按钮，方便退出触控通道模式。

说明：触控通道区域会露出底层 App，这是普通悬浮窗能稳定传递触摸的代价；主画面区域不再人为半透明叠底。
