# v0.1.3

修复左右眼缩放规则和手势缩放。

- 默认渲染改为每只眼按半屏宽度等比缩放，不再上下顶满。
- 例如屏幕宽度 1000 时，左眼区域为 0-500，中心 250；右眼区域为 500-1000，中心 750。
- 左右眼之间增加细分割线。
- 增加双指捏合缩放，默认 1.0，可在当前宽度适配基础上缩小或放大。
- APK 文件名为 `ParallelEyeConverter-v0.1.3-debug.apk`。
