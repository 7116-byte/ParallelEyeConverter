# v0.1.4

修复旋转、半屏裁剪和切应用后黑屏问题。

- 旋转横屏后刷新悬浮窗布局尺寸，不再沿用竖屏宽度导致画面很小。
- 双指放大后每只眼都裁剪在自己的半屏区域内，右眼不会盖到左眼。
- 中心分割线改为黑色，降低观感干扰。
- 从悬浮球重新打开时先停止旧录屏并重新拉起当前应用录屏授权，避免切到其他应用后继续使用旧授权流导致黑屏。
- APK 文件名为 `ParallelEyeConverter-v0.1.4-debug.apk`。
