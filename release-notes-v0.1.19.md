# v0.1.19 穿透模式控制修正

- `透` 模式下控制条自动收缩为右侧小 `控` 按钮，减少居中控制条遮挡和视觉叠加。
- `控` 按钮用于退出穿透模式；全屏画面在穿透后不会再接收双击，这是 Android 触摸穿透的系统行为。
- 继续保持穿透画面窗口 alpha 为 `1f`，尝试避免人为半透明叠底。

说明：如果设备系统强制让不可触摸悬浮窗半透明，应用侧无法强制完全不透明，只能在“可滑动”和“完全遮挡”之间折中。
